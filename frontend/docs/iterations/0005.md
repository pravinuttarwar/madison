# Iteration 5

Files changed: src/context/view-mode.tsx

## What changed

Build is clean. Done.

Changed the `ViewModeProvider` default in `src/context/view-mode.tsx` from `'monday'` to `'weekday'`, so the app now opens in **Weekday (today)** view by default. The Monday/Weekday toggle in the top bar still switches between them.
