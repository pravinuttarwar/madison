# Iteration 3

Files changed: src/components/AppShell.tsx

## What changed

Build is clean. Done.

## What changed

Consolidated the two separate top-bar controls (Dark toggle + Accessibility) into a single **Display** button, exactly per the client's recommended grouping.

- **Header row** is now `Monday recap | Weekday today | Display` — one control instead of three.
- **Button:** `⚙ Display` (sliders icon) — named "Display" so physician users don't read it as "not for me."
- **Panel title:** **Display & Accessibility**, with two grouped segmented controls:
  - **Theme** → Light / Dark
  - **Colors** → Standard / Color-Vision Friendly
- A one-line note reinforces that status is always shown by icon, shape and label (never color alone) and that we'd validate the exact palette together.

Implementation: replaced the old `DarkToggle` and `ColorVision` components with one `DisplayMenu` (plus a small reusable `Seg` segmented control). Theme flips the `.dark` token overrides; Colors applies/clears the color-vision-friendly CSS variables on `:root` — both behave identically to before, just unified behind one menu. The "Color-Vision Friendly" option maps to the red–green-friendly palette (the most common need); the structure leaves obvious room to add the "Contrast" and "Larger text" rows the client flagged as optional-later without reworking the layout.

`pnpm build` (tsc + vite) passes clean. Only `AppShell.tsx` was touched.
