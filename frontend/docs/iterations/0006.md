# Iteration 6

Files changed: src/App.tsx, src/components/AppShell.tsx, src/pages/Dashboard.tsx

## What changed

Build is clean. Done.

## What changed

**`src/pages/Dashboard.tsx`** — Priority Today rows are no longer "urgent-flagged": every row now uses a plain `CircleDot` icon and shows just title + detail + owner. No red alert icon, no "urgent" status pill. Deleted the `MondayView` and `OwnerSummary` blocks entirely; the Dashboard now renders just the today view (`TodayView`) and the data fetch is hardcoded to `getDashboard('weekday')`. Removed the now-unused `useViewMode`, `AlertTriangle`, `StatusPill`, and `Task` imports.

**`src/components/AppShell.tsx`** — Replaced the Monday/Weekday `ViewToggle` with a small static `TodayPill` (sun icon + "Today" label) where the toggle used to sit. Removed the `Connections` NavLink that lived in the header. Dropped the `Plug`, `CalendarClock`, `Sunrise` icons and the `useViewMode` import — all unused now. The user-chip date below the owner name is just `DATES.weekday` directly.

**`src/App.tsx`** — Removed the `/connections` route and the `Connections` page import so the link can't be reached by URL either.

`pnpm build` (tsc + vite) passes clean.
