{
  "name": "madison-command-center-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only backend-for-frontend for the Madison Command Center. Holds OAuth tokens, calls Microsoft Graph + QuickBooks read-only, transforms to the front-end DTO shapes. No PHI stored.",
  "scripts": {
    "start": "TZ=America/New_York node src/server.js",
    "dev": "TZ=America/New_York node --watch src/server.js",
    "test": "TZ=America/New_York node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
